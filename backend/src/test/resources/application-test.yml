# CI / @ActiveProfiles("test"): in-memory DB so Spring context loads without file locks.
spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: false

app:
  storage:
    base-dir: ${java.io.tmpdir}/transproj-test-data
